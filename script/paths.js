/**
 * config paths
 */

'use strict';

// const path = require('path');
// const resolvePath = (relativePath) => path.resolve(__dirname, relativePath);

module.exports = {
  buildPath: 'dist',
  publlicPath: '/'
}